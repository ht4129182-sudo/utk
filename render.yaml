services:
  - type: web
    name: utkarsh-toss-book-api
    env: node
    buildCommand: npm install --legacy-peer-deps --engine-strict=false --no-audit --no-fund && cd backend && npm install --legacy-peer-deps --engine-strict=false --no-audit --no-fund && cd ../frontend && npm install --legacy-peer-deps --engine-strict=false --no-audit --no-fund && npm run build
    startCommand: node backend/server.js
    envVars:
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        value: utkarsh_toss_book_secret_key_2024
      - key: NODE_ENV
        value: production
