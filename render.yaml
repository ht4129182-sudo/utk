services:
  - type: web
    name: utkarsh-toss-book-api
    env: node
    runtime: node
    plan: free
    buildCommand: cd frontend && npm install --legacy-peer-deps && npm run build
    startCommand: node backend/server.js
    envVars:
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        value: utkarsh_toss_book_secret_key_2024
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 18.20.0
    healthCheckPath: /health
