{
  "name": "utkarsh-toss-book",
  "version": "1.0.0",
  "description": "Cricket and Football Toss Betting Platform",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "npm install --legacy-peer-deps --engine-strict=false --no-audit --no-fund && cd backend && npm install --legacy-peer-deps --engine-strict=false --no-audit --no-fund && cd ../frontend && npm install --legacy-peer-deps --engine-strict=false --no-audit --no-fund && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "18.20.0"
  },
  "overrides": {
    "tar": "^6.2.1",
    "brace-expansion": "^2.0.1",
    "@tootallnate/once": "^2.0.1",
    "minimatch": "^3.1.2",
    "glob": "^8.1.0",
    "rimraf": "^3.0.2",
    "cacache": "^18.0.0",
    "make-fetch-happen": "^13.0.0",
    "node-gyp": "^10.0.0",
    "http-proxy-agent": "^5.0.0",
    "https-proxy-agent": "^5.0.0",
    "socks-proxy-agent": "^8.0.0",
    "semver": "^7.5.4",
    "esbuild": "^0.24.2",
    "rollup": "^4.18.0"
  }
}
