services:
  - type: web
    name: utkarsh-toss-book-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    routes:
      - type: rewrite
        source: /(.*)
        destination: /index.html
